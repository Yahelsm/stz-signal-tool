# strategy.py

import pandas as pd

class ShakedTzafoni:
    """
    Placeholder Shaked Tzafoni logic:
    - Sequence detection (higher-highs/lows, lower-highs/lows)
    - EMA8 & SMA20 confirmations
    - Candlestick patterns & volume filters
    """
    @staticmethod
    def check(df: pd.DataFrame) -> str:
        """
        Returns 'Enter', 'Breakout', 'Exit', or None.
        df columns: ['open', 'high', 'low', 'close', 'volume']
        """
        # TODO: fill in method from your Hebrew PDF instructions
        return None
